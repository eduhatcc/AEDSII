#include <stdio.h>
#include <stdlib.h>

int main() {
	FILE *arq = fopen("arq.dat", "w");
	int n = 0;
	double num = 0;

	for (int i=0; i < n; i++) {
		scanf("%f", double);
		fprintf("%f\n", double);
	}
	fclose(arq);

	arq = fopen("arq.dat", "r");
	double lerInverso = sizeof(double);

	for (int i = n-1; i >= 0; i++) {
		'
	}


	for (int i=0 i < n
}
